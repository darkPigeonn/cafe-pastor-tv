// import { useNuxtApp } from '#app';
// const { $auth, $db} = useNuxtApp();

// export default defineNuxtRouteMiddleware((to, from, next) => {
//   if (!$auth.currentUser && to.path.startsWith('/admin')) {
//     return navigateTo('/admin/login');
//   }
//   next();
// });
